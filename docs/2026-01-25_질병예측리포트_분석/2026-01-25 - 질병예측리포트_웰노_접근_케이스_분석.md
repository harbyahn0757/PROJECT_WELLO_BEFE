# 질병예측리포트 웰노 접근 케이스별 파이프라인 분석

**생성일**: 2026-01-25  
**작업일자**: 2026-01-25  
**작업내용**: 웰노 직접 접속 사용자의 질병예측리포트 접근 경로 파이프라인 분석  
**분석 대상**: 파트너가 아닌 웰노 사용자의 질병예측리포트 접근 경로

---

## 📋 개요

파트너가 아닌 웰노에 직접 접속한 사용자가 질병예측리포트에 접근할 때의 케이스별 파이프라인을 분석합니다.

---

## 🔍 접근 경로

### 경로 1: MainPage에서 질병예측 리포트 카드 클릭
- **위치**: `planning-platform/frontend/src/pages/MainPage.tsx`
- **함수**: `handleCardClick('prediction')` (line 912-1000)

### 경로 2: 직접 URL 접근
- **URL**: `/disease-report?uuid={uuid}&hospital={hospitalId}`
- **컴포넌트**: `DiseaseReportPage` (line 44)

---

## 📊 케이스별 파이프라인 분석

### 🎯 케이스 1: MainPage에서 카드 클릭 → 환자 정보 없음

**조건**:
- `uuid` 또는 `hospitalId`가 없음
- URL 파라미터, Context, Storage 모두에서 정보 없음

**파이프라인**:
```
1. MainPage.handleCardClick('prediction')
   ↓
2. 환자 정보 확인 (우선순위: URL → Context → Storage)
   ↓
3. ❌ 환자 정보 없음 감지
   ↓
4. alert('먼저 본인 인증을 진행해주세요.')
   ↓
5. navigate('/login?redirect=/disease-report')
   ↓
6. AuthForm에서 Tilko 인증 진행
   ↓
7. 인증 완료 후 /disease-report로 리다이렉트
```

**코드 위치**:
- `MainPage.tsx` line 934-939

**결과**: Tilko 인증 페이지로 이동

---

### 🎯 케이스 2: MainPage에서 카드 클릭 → 환자 정보 있음 → Mediarc 리포트 있음

**조건**:
- `uuid`와 `hospitalId` 있음
- `welno_mediarc_reports` 테이블에 리포트 존재

**파이프라인**:
```
1. MainPage.handleCardClick('prediction')
   ↓
2. ✅ 환자 정보 확인 (uuid, hospitalId)
   ↓
3. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
4. ✅ 응답: { success: true, has_report: true, data: {...} }
   ↓
5. navigate(`/disease-report?uuid=${uuid}&hospital=${hospitalId}`)
   ↓
6. DiseaseReportPage 마운트
   ↓
7. fetchReport() 실행
   ↓
8. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
9. 리포트 데이터 표시
```

**코드 위치**:
- `MainPage.tsx` line 952-960
- `DiseaseReportPage.tsx` line 502-556

**결과**: 질병예측리포트 페이지에 리포트 표시

---

### 🎯 케이스 3: MainPage에서 카드 클릭 → 환자 정보 있음 → Mediarc 리포트 없음 → 검진 데이터 있음

**조건**:
- `uuid`와 `hospitalId` 있음
- `welno_mediarc_reports` 테이블에 리포트 없음
- `welno_checkup_data` 테이블에 검진 데이터 있음

**파이프라인**:
```
1. MainPage.handleCardClick('prediction')
   ↓
2. ✅ 환자 정보 확인
   ↓
3. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
4. ❌ 응답: { success: false, has_report: false }
   ↓
5. GET /api/v1/welno/patient-health-data?uuid={uuid}&hospital_id={hospitalId}
   ↓
6. ✅ 응답: { success: true, data: { health_data: [...] } }
   ↓
7. navigate(`/disease-report?uuid=${uuid}&hospital=${hospitalId}&generate=true`)
   ↓
8. DiseaseReportPage 마운트 (generate=true)
   ↓
9. fetchReport() 실행
   ↓
10. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
    ↓
11. ❌ 리포트 없음 감지
    ↓
12. POST /api/v1/welno/mediarc-report/generate?uuid={uuid}&hospital_id={hospitalId}
    ↓
13. 백그라운드 리포트 생성 시작
    ↓
14. WebSocket으로 생성 완료 이벤트 대기
    ↓
15. 생성 완료 시 자동 재조회 및 리포트 표시
```

**코드 위치**:
- `MainPage.tsx` line 963-987
- `DiseaseReportPage.tsx` line 557-585

**결과**: 리포트 생성 요청 후 생성 완료 대기 → 리포트 표시

---

### 🎯 케이스 4: MainPage에서 카드 클릭 → 환자 정보 있음 → Mediarc 리포트 없음 → 검진 데이터 없음

**조건**:
- `uuid`와 `hospitalId` 있음
- `welno_mediarc_reports` 테이블에 리포트 없음
- `welno_checkup_data` 테이블에 검진 데이터 없음

**파이프라인**:
```
1. MainPage.handleCardClick('prediction')
   ↓
2. ✅ 환자 정보 확인
   ↓
3. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
4. ❌ 응답: { success: false, has_report: false }
   ↓
5. GET /api/v1/welno/patient-health-data?uuid={uuid}&hospital_id={hospitalId}
   ↓
6. ❌ 응답: 404 또는 { success: false }
   ↓
7. alert('건강 데이터가 없습니다. 먼저 건강검진 데이터를 수집해주세요.')
   ↓
8. navigate('/login?redirect=/disease-report')
   ↓
9. AuthForm에서 Tilko 인증 진행
   ↓
10. 인증 완료 후 검진 데이터 수집
    ↓
11. 데이터 수집 완료 후 /disease-report로 리다이렉트
```

**코드 위치**:
- `MainPage.tsx` line 968-994

**결과**: Tilko 인증 페이지로 이동하여 검진 데이터 수집 유도

---

### 🎯 케이스 5: 직접 URL 접근 → oid 파라미터 있음 (파트너 케이스)

**조건**:
- URL: `/disease-report?oid={oid}`
- 파트너 결제 주문번호 존재

**파이프라인**:
```
1. DiseaseReportPage 마운트
   ↓
2. oid 파라미터 확인
   ↓
3. GET /api/v1/campaigns/disease-prediction/report?oid={oid}
   ↓
4. ✅ 응답: { success: true, report_url: "...", mediarc_response: {...} }
   ↓
5. 리포트 데이터 표시 (파트너 리포트)
```

**코드 위치**:
- `DiseaseReportPage.tsx` line 444-491

**결과**: 파트너 리포트 표시 (웰노 사용자가 아닌 파트너 유입 케이스)

---

### 🎯 케이스 6: 직접 URL 접근 → uuid + hospitalId 있음 → Mediarc 리포트 있음

**조건**:
- URL: `/disease-report?uuid={uuid}&hospital={hospitalId}`
- `welno_mediarc_reports` 테이블에 리포트 존재

**파이프라인**:
```
1. DiseaseReportPage 마운트
   ↓
2. uuid, hospitalId 파라미터 확인
   ↓
3. fetchReport() 실행
   ↓
4. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
5. ✅ 응답: { success: true, has_report: true, data: {...} }
   ↓
6. 리포트 데이터 표시
```

**코드 위치**:
- `DiseaseReportPage.tsx` line 502-556

**결과**: 질병예측리포트 페이지에 리포트 표시

---

### 🎯 케이스 7: 직접 URL 접근 → uuid + hospitalId 있음 → Mediarc 리포트 없음 → generate=true

**조건**:
- URL: `/disease-report?uuid={uuid}&hospital={hospitalId}&generate=true`
- `welno_mediarc_reports` 테이블에 리포트 없음

**파이프라인**:
```
1. DiseaseReportPage 마운트
   ↓
2. uuid, hospitalId, generate=true 파라미터 확인
   ↓
3. fetchReport() 실행
   ↓
4. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
5. ❌ 응답: { success: false, has_report: false }
   ↓
6. shouldGenerate=true 감지
   ↓
7. POST /api/v1/welno/mediarc-report/generate?uuid={uuid}&hospital_id={hospitalId}
   ↓
8. 백그라운드 리포트 생성 시작
   ↓
9. WebSocket으로 생성 완료 이벤트 대기
   ↓
10. 생성 완료 시 자동 재조회 및 리포트 표시
```

**코드 위치**:
- `DiseaseReportPage.tsx` line 557-585

**결과**: 리포트 생성 요청 후 생성 완료 대기 → 리포트 표시

---

### 🎯 케이스 8: 직접 URL 접근 → uuid + hospitalId 있음 → Mediarc 리포트 없음 → generate 파라미터 없음

**조건**:
- URL: `/disease-report?uuid={uuid}&hospital={hospitalId}`
- `welno_mediarc_reports` 테이블에 리포트 없음
- `generate` 파라미터 없음

**파이프라인**:
```
1. DiseaseReportPage 마운트
   ↓
2. uuid, hospitalId 파라미터 확인
   ↓
3. fetchReport() 실행
   ↓
4. GET /api/v1/welno/mediarc-report?uuid={uuid}&hospital_id={hospitalId}
   ↓
5. ❌ 응답: { success: false, has_report: false }
   ↓
6. shouldGenerate=false (generate 파라미터 없음)
   ↓
7. setError('질병예측 리포트가 없습니다. 먼저 건강검진 데이터를 수집해주세요.')
   ↓
8. 에러 메시지 표시
```

**코드 위치**:
- `DiseaseReportPage.tsx` line 589-593

**결과**: 에러 메시지 표시 (리포트 없음 안내)

---

### 🎯 케이스 9: 직접 URL 접근 → uuid 또는 hospitalId 없음

**조건**:
- URL: `/disease-report` (파라미터 없음 또는 일부만 있음)
- Storage에도 정보 없음

**파이프라인**:
```
1. DiseaseReportPage 마운트
   ↓
2. uuid, hospitalId 파라미터 확인
   ↓
3. StorageManager에서 정보 확인
   ↓
4. ❌ uuid 또는 hospitalId 없음
   ↓
5. setError('환자 정보가 없습니다.')
   ↓
6. 에러 메시지 표시
```

**코드 위치**:
- `DiseaseReportPage.tsx` line 495-498

**결과**: 에러 메시지 표시 (환자 정보 없음 안내)

---

### 🎯 케이스 10: Tilko 인증 완료 후 리다이렉트

**조건**:
- Tilko 인증 완료
- `redirect=/disease-report` 파라미터 있음

**파이프라인**:
```
1. AuthForm에서 Tilko 인증 완료
   ↓
2. redirect 파라미터 확인: /disease-report
   ↓
3. generate=true 파라미터 자동 추가 (AuthForm.tsx line 195-197)
   ↓
4. navigate(`/disease-report?uuid=${uuid}&hospital=${hospitalId}&generate=true`)
   ↓
5. DiseaseReportPage 마운트
   ↓
6. 케이스 7과 동일한 플로우 진행
```

**코드 위치**:
- `AuthForm.tsx` line 195-197, 254-256

**결과**: 인증 완료 후 리포트 생성 페이지로 이동

---

## 🔄 백엔드 API 엔드포인트

### 1. Mediarc 리포트 조회
- **엔드포인트**: `GET /api/v1/welno/mediarc-report`
- **파라미터**: `uuid`, `hospital_id`
- **응답**: 
  ```json
  {
    "success": true,
    "has_report": true,
    "data": {
      "bodyage": 45,
      "rank": 1234,
      "disease_data": [...],
      "cancer_data": [...],
      "report_url": "..."
    }
  }
  ```
- **코드 위치**: `planning-platform/backend/app/api/v1/endpoints/welno_data.py` line 678-780

### 2. Mediarc 리포트 생성
- **엔드포인트**: `POST /api/v1/welno/mediarc-report/generate`
- **파라미터**: `uuid`, `hospital_id`
- **응답**:
  ```json
  {
    "success": true,
    "generating": true,
    "message": "리포트 생성이 시작되었습니다"
  }
  ```
- **코드 위치**: `planning-platform/backend/app/api/v1/endpoints/welno_data.py` line 782-905

### 3. 검진 데이터 조회
- **엔드포인트**: `GET /api/v1/welno/patient-health-data`
- **파라미터**: `uuid`, `hospital_id`
- **응답**:
  ```json
  {
    "success": true,
    "data": {
      "health_data": [...]
    }
  }
  ```

---

## 📝 주요 로직 요약

### 1. 환자 정보 우선순위
```
1순위: URL 파라미터 (uuid, hospital)
2순위: WelnoDataContext (patient?.uuid, patient?.hospital_id)
3순위: StorageManager (localStorage)
```

### 2. 데이터 체크 순서 (MainPage)
```
1. 환자 정보 확인
2. Mediarc 리포트 존재 여부 확인
3. 검진 데이터 존재 여부 확인
4. 각 단계별 적절한 액션 수행
```

### 3. 리포트 조회 로직 (DiseaseReportPage)
```
1. oid 파라미터 있음 → 파트너 리포트 조회
2. uuid + hospitalId 있음 → 웰노 리포트 조회
3. generate=true → 리포트 생성 요청
4. 리포트 없음 → 에러 메시지 표시
```

---

## ⚠️ 주의사항

1. **파트너 vs 웰노 구분**
   - `oid` 파라미터가 있으면 파트너 케이스로 처리
   - `uuid` + `hospitalId`만 있으면 웰노 케이스로 처리

2. **리포트 생성 중복 방지**
   - `generationRequestedRef`로 중복 요청 방지
   - WebSocket으로 생성 완료 이벤트 수신

3. **인증 리다이렉트**
   - `redirect=/disease-report` 파라미터가 있으면 인증 완료 후 해당 페이지로 이동
   - `generate=true` 파라미터 자동 추가 (AuthForm.tsx)

---

## 🎯 결론

파트너가 아닌 웰노 사용자의 질병예측리포트 접근은 다음과 같이 처리됩니다:

1. **환자 정보 없음** → Tilko 인증 유도
2. **리포트 있음** → 바로 표시
3. **리포트 없음 + 검진 데이터 있음** → 리포트 생성 요청
4. **리포트 없음 + 검진 데이터 없음** → Tilko 인증으로 검진 데이터 수집 유도

모든 케이스에서 적절한 안내와 리다이렉트가 이루어지며, 사용자 경험을 고려한 단계별 처리 로직이 구현되어 있습니다.
