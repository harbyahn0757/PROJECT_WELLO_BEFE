/**
 * AiSummaryButton — stub (Sub-task C 가 동일 경로 ../AiSummaryButton 에 실제 구현)
 * HeroBlock 이 이 stub 을 import. Sub-task C 완료 후 경로만 교체.
 */
interface AiSummaryButtonProps {
  uuid: string;
  hospitalId?: string;
}

export default function AiSummaryButton(_props: AiSummaryButtonProps) {
  return (
    <span className="report-view__ai-summary-stub">AI 요약 (Sub-task C)</span>
  );
}
