# 검진 설계 - 염려 항목 선택 화면 구성

## 📱 전체 화면 구조

```
┌─────────────────────────────────────┐
│  [←]  검진 항목 설계                  │  ← HealthTrendsHeader 재사용
│       마지막 업데이트: 2025-01-27     │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │  [검진] [병원] [약물]        │   │  ← 가로 슬라이드 탭 (UnifiedHealthTimeline group-slider 스타일)
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  ✓ 혈당                      │   │  ← 선택된 탭에 따라 항목 리스트 표시
│  │     2023-05-15 | 120 mg/dL  │   │     (CheckupRecommendationsPage 체크박스 스타일)
│  │     경계                      │   │
│  ├─────────────────────────────┤   │
│  │  ✓ 총콜레스테롤              │   │
│  │     2023-05-15 | 250 mg/dL  │   │
│  │     이상                      │   │
│  ├─────────────────────────────┤   │
│  │  □ LDL 콜레스테롤            │   │
│  │     2022-03-10 | 180 mg/dL  │   │
│  │     경계                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  선택한 항목: 2개            │   │  ← 선택 요약 섹션
│  └─────────────────────────────┘   │
│                                     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │    다음 단계로 진행하기       │   │  ← 하단 고정 버튼
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

## 🎨 상세 화면 구성

### 1. 헤더 영역 (HealthTrendsHeader 재사용)

```
┌─────────────────────────────────────┐
│  [←]  검진 항목 설계                  │
│       마지막 업데이트: 2025-01-27     │
└─────────────────────────────────────┘
```

**구현**:
- `HealthTrendsHeader` 컴포넌트 재사용
- `title="검진 항목 설계"`
- `onBack`: 메인 페이지로 이동
- `lastUpdateTime`: null (데이터 수집 시간 없음)

**스타일**:
```scss
// HealthTrendsHeader/styles.scss 재사용
height: 90px;
background: $background-cream; // #FEF9EE
padding: $spacing-md; // 16px
```

---

### 2. 탭 선택 영역 (가로 슬라이드)

```
┌─────────────────────────────────────┐
│  ┌─────────┬─────────┬─────────┐   │
│  │  검진    │  병원    │  약물    │   │  ← 활성 탭 강조
│  └─────────┴─────────┴─────────┘   │
└─────────────────────────────────────┘
```

**구현**:
- `UnifiedHealthTimeline`의 `group-slider` 스타일 재사용
- 가로 스크롤 가능한 탭 버튼
- 선택된 탭은 배경색 변경

**탭 타입**:
```typescript
type ConcernTab = 'checkup' | 'hospital' | 'medication';
```

**스타일**:
```scss
.concern-tab-slider {
  display: flex;
  gap: $spacing-sm; // 12px
  padding: $spacing-md $spacing-md $spacing-sm; // 16px 16px 12px
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none; // Firefox
  -ms-overflow-style: none; // IE
  
  &::-webkit-scrollbar {
    display: none; // Chrome
  }
}

.concern-tab {
  flex-shrink: 0;
  padding: $spacing-sm $spacing-md; // 12px 16px
  border-radius: $border-radius-lg; // 8px
  background: $white;
  border: 1px solid $border-beige; // #EAE4D7
  font-size: $font-base; // 16px
  font-weight: $font-weight-medium; // 500
  color: $gray-800; // #2d3748
  cursor: pointer;
  transition: all 0.2s ease;
  white-space: nowrap;
  
  &:hover {
    background: $brand-brown-light-bg; // #F8EDDA
    border-color: $brand-brown; // #7c746a
  }
  
  &--active {
    background: $brand-brown-dark; // #A16A51
    border-color: $brand-brown-dark;
    color: $white;
    font-weight: $font-weight-semibold; // 600
  }
}
```

---

### 3. 항목 리스트 영역 (체크박스)

#### 3-1. 검진 탭 선택 시

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │
│  │  ✓ 혈당                      │   │  ← 체크박스 (CheckupRecommendationsPage 스타일)
│  │     2023-05-15 | 120 mg/dL   │   │
│  │     [경계]                    │   │  ← 상태 뱃지 (TrendsSection status-badge 스타일)
│  │     서울대학교병원             │   │
│  ├─────────────────────────────┤   │
│  │  ✓ 총콜레스테롤              │   │
│  │     2023-05-15 | 250 mg/dL   │   │
│  │     [이상]                    │   │
│  │     서울대학교병원             │   │
│  ├─────────────────────────────┤   │
│  │  □ LDL 콜레스테롤            │   │
│  │     2022-03-10 | 180 mg/dL   │   │
│  │     [경계]                    │   │
│  │     세브란스병원               │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 3-2. 병원 탭 선택 시

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │
│  │  ✓ 서울대학교병원            │   │
│  │     2023-05-15 검진          │   │
│  │     이상 항목: 2개           │   │
│  ├─────────────────────────────┤   │
│  │  □ 세브란스병원              │   │
│  │     2022-03-10 검진          │   │
│  │     경계 항목: 1개           │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 3-3. 약물 탭 선택 시

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │
│  │  ✓ 아스피린                  │   │
│  │     2023-01-15 ~ 2023-06-15 │   │
│  │     서울대학교병원            │   │
│  ├─────────────────────────────┤   │
│  │  ✓ 메트포민                  │   │
│  │     2022-05-10 ~ 현재        │   │
│  │     세브란스병원              │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**구현**:
- `CheckupRecommendationsPage`의 체크박스 스타일 재사용
- 각 항목은 카드 형태로 표시
- 체크박스 클릭 시 선택/해제

**스타일**:
```scss
.concern-items-list {
  padding: $spacing-md; // 16px
  display: flex;
  flex-direction: column;
  gap: $spacing-sm; // 12px
}

.concern-item-card {
  background: $white;
  border-radius: $border-radius-xl; // 12px
  padding: $spacing-md; // 16px
  box-shadow: $shadow-sm;
  transition: all 0.2s ease;
  
  &:hover {
    box-shadow: $shadow-md;
  }
  
  &--selected {
    background: $brand-brown-light-bg; // #F8EDDA
    border: 1px solid $brand-brown-dark; // #A16A51
  }
}

.concern-item-checkbox-wrapper {
  display: flex;
  align-items: flex-start;
  gap: $spacing-sm; // 12px
}

.concern-item-checkbox {
  // CheckupRecommendationsPage 체크박스 스타일 재사용
  width: 20px;
  height: 20px;
  border-radius: $border-radius-sm; // 4px
  border: 2px solid $brand-brown-dark; // #A16A51
  background: $white;
  cursor: pointer;
  appearance: none;
  position: relative;
  flex-shrink: 0;
  margin-top: 2px; // 텍스트와 정렬
  
  &:checked {
    background: $brand-brown-dark;
    border-color: $brand-brown-dark;
    
    &::after {
      content: '✓';
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      color: $white;
      font-size: 14px;
      font-weight: $font-weight-bold;
    }
  }
}

.concern-item-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: $spacing-xs; // 8px
}

.concern-item-name {
  font-size: $font-base; // 16px
  font-weight: $font-weight-semibold; // 600
  color: $black;
}

.concern-item-meta {
  display: flex;
  align-items: center;
  gap: $spacing-xs; // 8px
  font-size: $font-sm; // 14px
  color: $gray-600; // #718096
}

.concern-item-status {
  // TrendsSection status-badge 스타일 재사용
  display: inline-flex;
  align-items: center;
  padding: 2px 8px;
  border-radius: $border-radius-sm; // 4px
  font-size: $font-xs; // 12px
  font-weight: $font-weight-medium; // 500
  
  &--warning {
    background: $badge-warning-bg; // #ed8936
    color: $badge-warning-text; // #ffffff
  }
  
  &--abnormal {
    background: $badge-abnormal-bg; // #f56565
    color: $badge-abnormal-text; // #ffffff
  }
}
```

---

### 4. 선택 요약 섹션

```
┌─────────────────────────────────────┐
│  ┌─────────────────────────────┐   │
│  │  선택한 항목: 2개            │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**구현**:
- 선택된 항목 개수 실시간 표시
- `CheckupRecommendationsPage`의 섹션 헤더 스타일 참고

**스타일**:
```scss
.selection-summary {
  margin: $spacing-lg $spacing-md; // 24px 16px
  padding: $spacing-md; // 16px
  background: $brand-brown-light-bg; // #F8EDDA
  border-radius: $border-radius-xl; // 12px
  text-align: center;
  box-shadow: $shadow-sm;
  
  &__count {
    font-size: $font-xl; // 20px
    font-weight: $font-weight-bold; // 700
    color: $brand-brown-darker; // #55433B
  }
  
  &__text {
    font-size: $font-sm; // 14px
    color: $gray-450; // #565656
    margin-top: $spacing-xs; // 8px
  }
}
```

---

### 5. 하단 버튼 (고정)

```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │    다음 단계로 진행하기       │   │  ← AppointmentLayout 하단 버튼 스타일
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**구현**:
- `AppointmentLayout`의 하단 버튼 스타일 재사용
- 선택된 항목이 없으면 비활성화

**스타일**:
```scss
.concern-selection-next-button {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  width: 100%;
  min-height: 56px; // 모바일: 52px
  background-color: $brand-brown-darker; // #55433B
  color: $white;
  border: none;
  border-radius: 0; // 하단 고정이므로
  font-size: 17px; // 모바일: 16px
  font-weight: $font-weight-semibold; // 600
  font-family: var(--font-family-greeting, 'Noto Sans KR', sans-serif);
  padding: 0.75rem 1rem; // 12px 16px
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 -4px 12px rgba($brand-brown-darker, 0.3);
  z-index: 100;
  
  &:hover:not(:disabled) {
    background-color: darken($brand-brown-darker, 5%);
  }
  
  &:disabled {
    background-color: $gray-400; // #cbd5e0
    color: $gray-600; // #718096
    cursor: not-allowed;
    opacity: 0.6;
  }
}
```

---

## 🔄 상태 관리

### 탭 상태
```typescript
const [activeTab, setActiveTab] = useState<ConcernTab>('checkup');
```

### 선택 항목 상태
```typescript
const [selectedItems, setSelectedItems] = useState<Set<string>>(new Set());
```

### 필터링된 항목
```typescript
const filteredItems = useMemo(() => {
  switch (activeTab) {
    case 'checkup':
      return abnormalCheckupItems; // 정상이 아닌 검진 항목
    case 'hospital':
      return hospitalItems; // 병원별 그룹화된 항목
    case 'medication':
      return medicationItems; // 약물 복용 이력
    default:
      return [];
  }
}, [activeTab, healthData, prescriptionData]);
```

---

## 📱 반응형 디자인

### 모바일 (기본)
- 탭: 가로 스크롤 가능
- 항목 카드: 전체 너비
- 하단 버튼: 고정, 높이 52px

### 태블릿 이상
- 탭: 중앙 정렬, 최대 너비 600px
- 항목 카드: 그리드 레이아웃 (2열)
- 하단 버튼: 높이 56px

---

## 🎯 데이터 구조

### 검진 항목
```typescript
interface CheckupConcernItem {
  id: string;
  name: string; // '혈당', '총콜레스테롤' 등
  value: number;
  unit: string; // 'mg/dL', 'cm' 등
  date: string; // '2023-05-15'
  location: string; // '서울대학교병원'
  status: 'warning' | 'abnormal';
  checkupDate: string;
}
```

### 병원 항목
```typescript
interface HospitalConcernItem {
  id: string;
  hospitalName: string;
  checkupDate: string;
  abnormalCount: number;
  warningCount: number;
  items: CheckupConcernItem[];
}
```

### 약물 항목
```typescript
interface MedicationConcernItem {
  id: string;
  medicationName: string;
  startDate: string;
  endDate?: string;
  period: string; // '2023-01-15 ~ 2023-06-15'
  hospitalName: string;
  prescriptionDate: string;
}
```

---

## ✅ 구현 체크리스트

- [ ] HealthTrendsHeader 컴포넌트 재사용
- [ ] UnifiedHealthTimeline의 group-slider 스타일 재사용
- [ ] CheckupRecommendationsPage의 체크박스 스타일 재사용
- [ ] TrendsSection의 status-badge 스타일 재사용
- [ ] AppointmentLayout의 하단 버튼 스타일 재사용
- [ ] 탭 전환 애니메이션 (fadeInUp)
- [ ] 체크박스 선택 애니메이션
- [ ] 선택 요약 실시간 업데이트
- [ ] 빈 상태 처리 (항목이 없을 때)
- [ ] 로딩 상태 처리

