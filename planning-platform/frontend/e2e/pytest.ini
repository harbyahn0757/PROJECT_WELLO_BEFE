[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    requires_server: marks tests requiring live server
    requires_db: marks tests requiring DB tunnel
