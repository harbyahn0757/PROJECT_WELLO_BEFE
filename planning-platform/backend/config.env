# 건강검진 관리 시스템 환경변수

# 환경 설정
DEBUG=true
ENVIRONMENT=development
API_VERSION=v1
API_PREFIX=/api/v1

# 서버 설정
HOST=0.0.0.0
PORT=8082

# 보안 설정
SECRET_KEY=ThrThr!23
ACCESS_TOKEN_EXPIRE_MINUTES=20

# CORS 설정
CORS_ALLOWED_ORIGINS=["http://localhost:9283"]
CORS_ALLOWED_METHODS=["*"]
CORS_ALLOWED_HEADERS=["*"]

# 카카오 API 설정 (레퍼런스에서 가져옴)
KAKAO_CLIENT_ID=617eb8132a5297cd0856de888458f22b
KAKAO_CLIENT_SECRET=your-kakao-client-secret
KAKAO_REDIRECT_URI=http://localhost:9283/auth/kakao/callback

# 틸코 API 설정 (레퍼런스에서 가져옴)
TILKO_API_HOST=https://api.tilko.net
TILKO_API_KEY=8d5421c1ebfb42f38a6aac24c02db894
TILKO_AES_KEY=kj1bAvp861uMvuooHrqxgvqqcldwcj9Q
TILKO_AES_IV=0000000000000000

# OpenAI GPT 설정
OPENAI_API_KEY=your-openai-api-key
GPT_MODEL=gpt-4
MAX_TOKENS=2000

# 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=health_check_db
DB_USER=admin
DB_PASSWORD=your-db-password

# Redis 설정 (세션 관리용)
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=your-redis-password

# 시스템 기본값
DEFAULT_LAYOUT=vertical
DEFAULT_LANGUAGE=ko
DEFAULT_TIMEZONE=Asia/Seoul

# 검진 설정
MIN_CHECKUP_AGE=19
MAX_CHECKUP_AGE=120
DEFAULT_CHECKUP_DURATION_MINUTES=120

# 알림 설정
NOTIFICATION_SENDER=no-reply@healthcheck.com
SMS_SENDER_NUMBER=15881234

# 파일 업로드 설정
MAX_UPLOAD_SIZE=10485760  # 10MB
ALLOWED_FILE_TYPES=["image/jpeg", "image/png", "application/pdf"]
UPLOAD_PATH=/uploads

# 로깅 설정
LOG_LEVEL=info
LOG_FORMAT=json
LOG_PATH=/var/log/healthcheck
