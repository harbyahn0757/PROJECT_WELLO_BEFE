# 페르소나 판정 및 점수 매기기 가이드

**작성일**: 2025-01-07  
**파일 위치**: `planning-platform/backend/app/services/checkup_design/persona.py`

---

## 📋 개요

검진 설계 시스템은 문진 응답을 기반으로 5가지 페르소나 유형을 판정합니다. 각 페르소나는 특정 문진 항목과 값에 따라 점수를 받으며, 최종적으로 가장 높은 점수를 받은 페르소나가 선택됩니다.

---

## 🎯 페르소나 유형 및 우선순위

### 우선순위 (점수 동점 시 우선 적용)

1. **Worrier (가족력/불안형)** - 가장 강력한 트리거
2. **Symptom Solver (증상해결형)**
3. **Manager (만성질환 관리형)**
4. **Optimizer (웰니스/활력형)**
5. **Minimalist (실속형)** - 기본값 (다른 페르소나 점수가 낮을 때)

---

## 📝 문진 항목별 상세 가이드

### 문진 항목 구조

```python
survey_responses = {
    "weight_change": "decrease_bad",           # 체중 변화
    "daily_routine": ["desk_job"],            # 일상 생활 패턴 (배열 가능)
    "exercise_frequency": "regular",           # 운동 빈도
    "smoking": "current_smoker",              # 흡연 상태
    "drinking": "weekly_3plus",               # 음주 빈도
    "sleep_hours": "less_5",                  # 수면 시간
    "stress_level": "very_high",              # 스트레스 수준
    "family_history": ["cancer", "diabetes"],  # 가족력 (배열)
    "colonoscopy_experience": "no_afraid",    # 대장내시경 경험
    "additional_concerns": "최근 두통이 심합니다"  # 추가 걱정사항 (자유 텍스트)
}
```

---

## 1️⃣ Worrier (가족력/불안형)

### 판정 기준
- **기본 점수**: 가족력이 있으면 **100점** (최고 점수)
- **Minimalist 점수**: 0점으로 설정 (다른 페르소나보다 우선)

### 문진 항목: `family_history` (배열)

#### 가능한 값
- `"none"`: 가족력 없음 → Worrier 점수 없음
- `"cancer"`: 암 가족력 → **+20점** (추가 가중치)
- `"stroke"`: 뇌졸중 가족력 → **+15점**
- `"heart_disease"`: 심장질환 가족력 → **+15점**
- `"diabetes"`: 당뇨병 가족력 → 기본 점수만
- `"hypertension"`: 고혈압 가족력 → 기본 점수만
- 기타 질환명

#### 점수 계산 예시
```python
# 예시 1: 암 가족력만 있는 경우
family_history = ["cancer"]
→ Worrier: 100 + 20 = 120점

# 예시 2: 암 + 뇌졸중 가족력
family_history = ["cancer", "stroke"]
→ Worrier: 100 + 20 + 15 = 135점

# 예시 3: 가족력 없음
family_history = ["none"] 또는 []
→ Worrier: 0점
```

#### 코드 위치
```python
# persona.py 65-74번 라인
family_history = survey_responses.get("family_history", [])
if family_history and "none" not in family_history:
    scores["Worrier"] = 100  # 최고 점수
    scores["Minimalist"] = 0
    
    # 가족력 종류별 가중치
    if "cancer" in family_history:
        scores["Worrier"] += 20  # 암 가족력은 더 강력
    if "stroke" in family_history or "heart_disease" in family_history:
        scores["Worrier"] += 15
```

---

## 2️⃣ Symptom Solver (증상해결형)

### 판정 기준
- 증상이나 건강 문제를 해결하려는 의지가 강한 타입
- 여러 항목에서 점수를 받을 수 있음 (누적)

### 문진 항목별 점수

#### Q1: `weight_change` - 체중 변화

**가능한 값**:
- `"decrease_bad"`: 의도치 않은 체중 감소 → **+50점**
- `"decrease_good"`: 의도한 체중 감소 (다이어트 성공) → Optimizer로 이동
- `"increase_some"`: 약간 증가 → Manager로 이동
- `"increase_more"`: 많이 증가 → Manager로 이동
- `"stable"`: 유지 → 점수 없음

**코드 위치**: `persona.py` 81-84번 라인

---

#### Q6: `sleep_hours` - 수면 시간

**가능한 값**:
- `"less_5"`: 5시간 미만 → **+30점**
- `"5_6"`: 5-6시간 → 점수 없음 (추후 추가 가능)
- `"7_8"`: 7-8시간 → 점수 없음
- `"more_9"`: 9시간 이상 → 점수 없음

**코드 위치**: `persona.py` 87-90번 라인

---

#### Q2/Q7: `daily_routine` - 일상 생활 패턴 (배열 가능)

**가능한 값** (배열 또는 단일 문자열):
- `"mental_stress"`: 정신적 압박이 많은 직업 → **+40점** (Symptom Solver), **+15점** (Worrier)
- `"service_job"`: 감정 소모가 많은 서비스직 → **+40점** (Symptom Solver)
- `"physical_job"`: 육체노동 → **+20점** (Symptom Solver)
- `"desk_job"`: 사무직 → Optimizer로 이동
- `"irregular"`: 불규칙한 생활 → Manager로 이동

**코드 위치**: `persona.py` 94-106번 라인

**주의사항**: 
- `daily_routine`은 배열 또는 단일 문자열로 올 수 있음
- 배열인 경우 `any()` 함수로 포함 여부 확인

---

#### Q10: `additional_concerns` - 추가 걱정사항 (자유 텍스트)

**증상 키워드 감지**:
다음 키워드가 포함되어 있으면 **+35점**

```python
symptom_keywords = [
    "통증", "아픔", "아파", "불편", "두통", 
    "피로", "어지럼", "답답", "저림", 
    "숨", "가슴", "배", "허리", "무릎", 
    "관절", "소화", "변비", "설사"
]
```

**코드 위치**: `persona.py` 109-114번 라인

**예시**:
```python
additional_concerns = "최근 두통이 심합니다"
→ "두통" 키워드 감지 → +35점

additional_concerns = "가슴이 답답하고 숨이 차요"
→ "가슴", "답답", "숨" 키워드 감지 → +35점
```

---

### Symptom Solver 점수 계산 예시

```python
# 예시 1: 체중 감소 + 수면 부족
weight_change = "decrease_bad"  # +50점
sleep_hours = "less_5"          # +30점
→ Symptom Solver: 80점

# 예시 2: 정신적 스트레스 + 증상 키워드
daily_routine = ["mental_stress"]  # +40점
additional_concerns = "두통이 심합니다"  # +35점
→ Symptom Solver: 75점

# 예시 3: 복합 조건
weight_change = "decrease_bad"     # +50점
sleep_hours = "less_5"              # +30점
daily_routine = ["service_job"]     # +40점
additional_concerns = "피로가 심합니다"  # +35점
→ Symptom Solver: 155점
```

---

## 3️⃣ Manager (만성질환 관리형)

### 판정 기준
- 만성질환 위험 요인(흡연, 음주, 비만 등)을 관리해야 하는 타입
- 여러 위험 요인이 복합될 때 추가 가중치

### 문진 항목별 점수

#### Q4: `smoking` - 흡연 상태

**가능한 값**:
- `"current_smoker"`: 현재 흡연 중 → **+40점**
- `"past_smoker"`: 과거 흡연 → 점수 없음 (추후 추가 가능)
- `"never"`: 비흡연 → 점수 없음

**코드 위치**: `persona.py` 121-124번 라인

---

#### Q5: `drinking` - 음주 빈도

**가능한 값**:
- `"weekly_3plus"`: 주 3회 이상 → **+30점**
- `"weekly_1_2"`: 주 1-2회 → **+30점**
- `"monthly"`: 월 1-2회 → 점수 없음
- `"rarely"`: 거의 안 마심 → 점수 없음
- `"never"`: 전혀 안 마심 → 점수 없음

**코드 위치**: `persona.py` 127-130번 라인

---

#### Q1: `weight_change` - 체중 변화 (Manager용)

**가능한 값**:
- `"increase_some"`: 약간 증가 → **+25점**
- `"increase_more"`: 많이 증가 → **+25점**

**코드 위치**: `persona.py` 132-135번 라인

---

#### Q7: `daily_routine` - 불규칙한 생활

**가능한 값**:
- `"irregular"`: 불규칙한 생활 → **+20점**

**코드 위치**: `persona.py` 139-141번 라인

---

#### 복합 위험 가중치 (추가 점수)

**조건**: 흡연 + 음주 + 비만이 모두 만족될 때
```python
smoking == "current_smoker" AND
drinking in ["weekly_1_2", "weekly_3plus"] AND
weight_change == "increase_more"
→ 추가 +30점
```

**코드 위치**: `persona.py` 143-147번 라인

---

### Manager 점수 계산 예시

```python
# 예시 1: 흡연만 있는 경우
smoking = "current_smoker"
→ Manager: 40점

# 예시 2: 흡연 + 음주
smoking = "current_smoker"      # +40점
drinking = "weekly_3plus"        # +30점
→ Manager: 70점

# 예시 3: 복합 위험 (추가 가중치)
smoking = "current_smoker"      # +40점
drinking = "weekly_3plus"        # +30점
weight_change = "increase_more"  # +25점
→ 복합 조건 만족 → +30점 (추가)
→ Manager: 125점
```

---

## 4️⃣ Optimizer (웰니스/활력형)

### 판정 기준
- 건강을 적극적으로 관리하고 최상의 컨디션을 추구하는 타입
- 고소득 + 규칙적 운동 등이 복합될 때 추가 가중치

### 문진 항목별 점수

#### Q3: `exercise_frequency` - 운동 빈도

**가능한 값**:
- `"regular"`: 규칙적으로 운동 → **+40점**
- `"sometimes"`: 가끔 운동 → 점수 없음 (추후 추가 가능)
- `"rarely"`: 거의 안 함 → 점수 없음
- `"never"`: 전혀 안 함 → 점수 없음

**코드 위치**: `persona.py` 154-156번 라인

---

#### Q2: `daily_routine` - 고소득 직업군

**가능한 값** (배열 또는 단일 문자열):
- `"desk_job"`: 사무직 → **+30점**
- `"mental_stress"`: 정신적 압박이 많은 직업 → **+30점** (Optimizer용)

**주의**: `"mental_stress"`는 Symptom Solver에도 점수를 주지만, Optimizer에도 점수를 줌

**코드 위치**: `persona.py` 158-163번 라인

---

#### Q1: `weight_change` - 다이어트 성공

**가능한 값**:
- `"decrease_good"`: 의도한 체중 감소 (다이어트 성공) → **+25점**
- `"Minimalist"` 점수: 0점으로 설정

**코드 위치**: `persona.py` 165-168번 라인

---

#### 복합 조건 (추가 가중치)

**조건**: 규칙적 운동 + 정신적 스트레스 직업
```python
exercise_frequency == "regular" AND
"mental_stress" in daily_routine
→ 추가 +20점
```

**코드 위치**: `persona.py` 170-172번 라인

---

### Optimizer 점수 계산 예시

```python
# 예시 1: 규칙적 운동만
exercise_frequency = "regular"
→ Optimizer: 40점

# 예시 2: 규칙적 운동 + 사무직
exercise_frequency = "regular"  # +40점
daily_routine = ["desk_job"]    # +30점
→ Optimizer: 70점

# 예시 3: 복합 조건 (추가 가중치)
exercise_frequency = "regular"      # +40점
daily_routine = ["mental_stress"]  # +30점
→ 복합 조건 만족 → +20점 (추가)
→ Optimizer: 90점

# 예시 4: 다이어트 성공
weight_change = "decrease_good"  # +25점
→ Optimizer: 25점
```

---

## 5️⃣ Minimalist (실속형)

### 판정 기준
- 기본값: **100점**으로 시작
- 다른 페르소나가 점수를 받으면 **0점**으로 설정
- 모든 페르소나 점수가 낮을 때만 선택됨

### Minimalist 점수가 0점으로 설정되는 조건

다음 조건 중 하나라도 만족하면 Minimalist는 0점:

1. **가족력이 있는 경우** (`family_history`에 "none"이 아닌 값)
2. **체중 변화가 있는 경우** (감소/증가 모두)
3. **수면 부족** (`sleep_hours == "less_5"`)
4. **스트레스가 많은 직업** (`mental_stress`, `service_job`, `physical_job`)
5. **증상 키워드가 포함된 경우** (`additional_concerns`에 증상 키워드)
6. **흡연 중** (`smoking == "current_smoker"`)
7. **음주 빈도가 높은 경우** (`drinking in ["weekly_1_2", "weekly_3plus"]`)
8. **불규칙한 생활** (`daily_routine`에 `"irregular"` 포함)
9. **다이어트 성공** (`weight_change == "decrease_good"`)

---

## 🔄 점수 계산 로직

### 1. 초기화
```python
scores = {
    "Worrier": 0,
    "Symptom Solver": 0,
    "Manager": 0,
    "Optimizer": 0,
    "Minimalist": 100  # 기본값
}
```

### 2. 순차적 점수 부여
1. Worrier 점수 계산 (가족력 확인)
2. Symptom Solver 점수 계산 (증상/문제 해결)
3. Manager 점수 계산 (만성질환 위험)
4. Optimizer 점수 계산 (웰니스/활력)
5. Minimalist는 다른 페르소나가 점수를 받으면 0점

### 3. 최종 판정
```python
primary_persona = max(scores, key=scores.get)
```

가장 높은 점수를 받은 페르소나가 선택됩니다.

---

## 📊 실제 계산 예시

### 예시 1: Worrier (가족력 우선)

```python
survey_responses = {
    "family_history": ["cancer", "stroke"],
    "weight_change": "stable",
    "smoking": "never",
    "drinking": "rarely"
}

# 점수 계산
Worrier: 100 (기본) + 20 (암) + 15 (뇌졸중) = 135점
Symptom Solver: 0점
Manager: 0점
Optimizer: 0점
Minimalist: 0점 (가족력 있음)

→ 최종: Worrier (135점)
```

---

### 예시 2: Symptom Solver (증상 중심)

```python
survey_responses = {
    "family_history": [],
    "weight_change": "decrease_bad",
    "sleep_hours": "less_5",
    "daily_routine": ["service_job"],
    "additional_concerns": "두통이 심합니다"
}

# 점수 계산
Worrier: 0점
Symptom Solver: 50 (체중 감소) + 30 (수면 부족) + 40 (서비스직) + 35 (증상 키워드) = 155점
Manager: 0점
Optimizer: 0점
Minimalist: 0점 (여러 조건 만족)

→ 최종: Symptom Solver (155점)
```

---

### 예시 3: Manager (만성질환 위험)

```python
survey_responses = {
    "family_history": [],
    "smoking": "current_smoker",
    "drinking": "weekly_3plus",
    "weight_change": "increase_more"
}

# 점수 계산
Worrier: 0점
Symptom Solver: 0점
Manager: 40 (흡연) + 30 (음주) + 25 (비만) + 30 (복합 가중치) = 125점
Optimizer: 0점
Minimalist: 0점

→ 최종: Manager (125점)
```

---

### 예시 4: Optimizer (웰니스)

```python
survey_responses = {
    "family_history": [],
    "exercise_frequency": "regular",
    "daily_routine": ["mental_stress"],
    "weight_change": "decrease_good"
}

# 점수 계산
Worrier: 0점
Symptom Solver: 0점
Manager: 0점
Optimizer: 40 (운동) + 30 (정신적 스트레스) + 20 (복합 가중치) + 25 (다이어트 성공) = 115점
Minimalist: 0점

→ 최종: Optimizer (115점)
```

---

### 예시 5: Minimalist (기본값)

```python
survey_responses = {
    "family_history": [],
    "weight_change": "stable",
    "smoking": "never",
    "drinking": "rarely",
    "exercise_frequency": "sometimes",
    "sleep_hours": "7_8"
}

# 점수 계산
Worrier: 0점
Symptom Solver: 0점
Manager: 0점
Optimizer: 0점
Minimalist: 100점 (기본값 유지)

→ 최종: Minimalist (100점)
```

---

## 🎯 페르소나별 특성

### Worrier (가족력/불안형)
- **업셀링 강도**: very_high
- **Bridge Strategy**: Peace of Mind
- **톤앤매너**: 공감, 안심, 확신
- **설득 메시지**: "가족력 때문에 불안하시죠? 눈으로 확인하고 마음의 짐을 덜으세요."

### Symptom Solver (증상해결형)
- **업셀링 강도**: high
- **Bridge Strategy**: Gap Filling
- **톤앤매너**: 분석적, 해결책 제시
- **설득 메시지**: "단순 피로가 아닙니다. 숨겨진 원인을 데이터로 찾아야 합니다."

### Manager (만성질환 관리형)
- **업셀링 강도**: medium
- **Bridge Strategy**: Linkage
- **톤앤매너**: 경고, 관리, 체계적
- **설득 메시지**: "음주와 비만이 만나면 간이 굳어집니다. 연결고리를 끊어야 합니다."

### Optimizer (웰니스/활력형)
- **업셀링 강도**: very_high
- **Bridge Strategy**: Vitality
- **톤앤매너**: 프리미엄, 최신지견
- **설득 메시지**: "병이 없는 것과 활력이 넘치는 건 다릅니다. 최상의 컨디션을 만드세요."

### Minimalist (실속형)
- **업셀링 강도**: low
- **Bridge Strategy**: Efficiency
- **톤앤매너**: 간결, 핵심, 가성비
- **설득 메시지**: "바쁘시겠지만, 가성비 있게 딱 이것 하나만 챙기시면 됩니다."

---

## 🔍 확인 필요 사항

### 1. 문진 항목 값 확인
현재 코드에서 사용하는 값들이 실제 프론트엔드에서 전달되는 값과 일치하는지 확인 필요:
- `weight_change`의 정확한 값 목록
- `daily_routine`의 정확한 값 목록 (배열인지 단일 문자열인지)
- `exercise_frequency`의 정확한 값 목록
- `smoking`, `drinking`, `sleep_hours`의 정확한 값 목록

### 2. 누락된 문진 항목
다음 항목들이 코드에 반영되지 않았을 수 있음:
- `stress_level`: 현재 코드에서 사용되지 않음
- `colonoscopy_experience`: 현재 코드에서 사용되지 않음
- 기타 추가 문진 항목

### 3. 점수 조정 필요 여부
현재 점수 체계가 적절한지 검토 필요:
- Worrier의 100점 기본 점수가 너무 높은지
- 복합 조건의 추가 가중치가 적절한지
- Minimalist의 기본값 100점이 적절한지

---

## 📝 개선 제안

### 1. 문진 항목 값 상수화
```python
# constants.py에 추가
WEIGHT_CHANGE_VALUES = {
    "DECREASE_BAD": "decrease_bad",
    "DECREASE_GOOD": "decrease_good",
    "INCREASE_SOME": "increase_some",
    "INCREASE_MORE": "increase_more",
    "STABLE": "stable"
}

SMOKING_VALUES = {
    "CURRENT": "current_smoker",
    "PAST": "past_smoker",
    "NEVER": "never"
}
```

### 2. 점수 가중치 상수화
```python
# constants.py에 추가
PERSONA_SCORES = {
    "WORRIER_BASE": 100,
    "WORRIER_CANCER_BONUS": 20,
    "WORRIER_STROKE_BONUS": 15,
    "SYMPTOM_WEIGHT_LOSS": 50,
    "SYMPTOM_SLEEP_LESS": 30,
    # ...
}
```

### 3. 로깅 추가
페르소나 판정 과정을 로그로 남겨 디버깅 용이하게:
```python
logger.info(f"페르소나 점수 계산: {scores}")
logger.info(f"최종 선택: {primary_persona} ({scores[primary_persona]}점)")
```

---

**다음 단계**: 사용자가 문진 항목 값을 정리해주시면 코드에 반영하겠습니다.

